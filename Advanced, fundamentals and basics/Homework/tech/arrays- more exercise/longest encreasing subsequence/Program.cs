using System;
using System.Linq;

namespace longest_encreasing_subsequence
{
    class Program
    {
        static void Main(string[] args)
        {
            //To be continiu
            int[] array = Console.ReadLine()
                .Trim()
                .Split(" ")
                .Select(int.Parse)
                .ToArray();
            int possitionLenght = 0;
            int[] len = new int[array.Length];
            for (int i = 0; i < array.Length; i++)
            {

            }
        }
    }
}
